<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>給与明細 | Attendly</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <header class="app-header">
    <div class="app-header__brand">
      <h1 class="app-header__title">
        <a href="/employee" class="app-header__home-link">Attendly</a>
      </h1>
    </div>
    <button class="menu-toggle" type="button" aria-label="メニューを開閉" aria-expanded="false">
      <span class="menu-toggle__bar"></span>
      <span class="menu-toggle__bar"></span>
      <span class="menu-toggle__bar"></span>
    </button>
    <nav class="app-header__nav">
      <span class="nav-user"><%= currentUser.username %>さん</span>
      <a href="/employee" class="nav-link">勤怠ダッシュボード</a>
      <a href="/employee/payrolls" class="nav-link">給与明細</a>
      <a href="/password/change" class="nav-link">パスワード変更</a>
      <form method="post" action="/logout" class="nav-form">
        <input type="hidden" name="_csrf" value="<%= csrfToken %>">
        <button type="submit" class="btn link">ログアウト</button>
      </form>
    </nav>
  </header>
  <main class="container">
    <% if (flash) { %>
      <div class="flash <%= flash.type %>"><%= flash.message %></div>
    <% } %>

    <section class="card">
      <h2>給与明細一覧</h2>
      <p class="form-note">
        受信済みの給与明細は以下に表示されます。ダウンロードして保管するか、印刷してください。
        PDF 以外（Excel 等）のファイルはブラウザでプレビューできないため「印刷」を押してもダウンロードになります。
      </p>
      <div class="table-responsive">
        <table class="table">
          <thead>
            <tr>
              <th>送信時刻</th>
              <th>ファイル名</th>
              <th>サイズ</th>
              <th>操作</th>
            </tr>
          </thead>
          <tbody>
            <% if (!payrollRecords || payrollRecords.length === 0) { %>
              <tr><td colspan="4">給与明細はまだ届いていません。送信されるとここに表示されます。</td></tr>
            <% } else { %>
            <% payrollRecords.forEach(function(record) { %>
              <tr>
                <td class="table-text-nowrap"><%= record.sentAtDisplay %></td>
                <td class="table-col-file table-text-nowrap"><%= record.originalFileName %></td>
                <td class="table-text-nowrap"><%= record.fileSizeDisplay || '-' %></td>
                <td>
                  <a class="btn secondary" href="<%= record.downloadUrl %>">ダウンロード</a>
                  <a class="btn link" href="<%= record.inlineUrl %>" target="_blank" rel="noopener">印刷</a>
                </td>
              </tr>
              <% }); %>
            <% } %>
          </tbody>
        </table>
      </div>
      <% if (payrollRecords && payrollRecords.length > 0) { %>
        <p class="form-note">印刷ボタンは新しいタブで開きます。ブラウザの印刷機能をご利用ください。</p>
      <% } %>
    </section>
  </main>
  <script src="/header.js" defer></script>
</body>
</html>
