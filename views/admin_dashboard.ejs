<!DOCTYPE html>
<html lang="ja">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>管理者ダッシュボード | 勤怠管理システム</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <header class="app-header">
    <h1>勤怠管理システム - 管理者</h1>
    <nav>
      <a href="/admin/users/new" class="nav-link">従業員アカウント作成</a>
      <form method="post" action="/logout" class="nav-form">
        <button type="submit" class="btn link">ログアウト</button>
      </form>
    </nav>
  </header>
  <main class="container">
    <% if (flash) { %>
      <div class="flash <%= flash.type %>"><%= flash.message %></div>
    <% } %>

    <section class="card">
      <h2>月次サマリー</h2>
      <form method="get" action="/admin" class="form form-inline">
        <label>
          <span>年</span>
          <input type="number" name="year" value="<%= targetYear %>" min="2000" max="2100">
        </label>
        <label>
          <span>月</span>
          <input type="number" name="month" value="<%= targetMonth %>" min="1" max="12">
        </label>
        <button type="submit" class="btn">表示</button>
      </form>
      <table class="table">
        <thead>
          <tr>
            <th>従業員</th>
            <th>月合計</th>
            <th>Excel出力</th>
          </tr>
        </thead>
        <tbody>
          <% if (monthlySummary.length === 0) { %>
            <tr><td colspan="3">従業員が登録されていません。</td></tr>
          <% } else { %>
            <% monthlySummary.forEach(function(row) { %>
              <tr>
                <td><%= row.user.username %></td>
                <td><%= row.formattedTotal %></td>
                <td>
                  <a class="btn secondary" href="/admin/export?userId=<%= row.user.id %>&year=<%= targetYear %>&month=<%= targetMonth %>">Excel出力</a>
                </td>
              </tr>
            <% }); %>
          <% } %>
        </tbody>
      </table>
    </section>
  </main>
</body>
</html>

